import React from "react";
import classes from "./logo.css";

const logo = () => {
    return(
        <div className={classes.logo}>
            <h1>testing</h1>
        </div>
    );
}

export default logo;